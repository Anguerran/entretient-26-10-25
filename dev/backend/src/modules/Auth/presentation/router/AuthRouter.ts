// import { Router } from "express";
// import { DBAuthRepository } from "../../infrastructure/repository/DBAuthRepository";
// import { AuthController } from "../controller/AuthController";
// import { AuthRoutes } from "./AuthRoutes";
// // import { googleStrategyService } from "../../infrastructure/service/passport/strategy/google/googleStrategy";

// const repository = new DBAuthRepository();
// const controller = new AuthController(repository);
// const router = Router();
// //*  /Auth/refreshToken
// router.get(AuthRoutes.refreshToken, controller.refreshToken.bind(controller));
// //* /Auth/signup
// router.post(AuthRoutes.signup, controller.signUp.bind(controller));
// //* /Auth/login/local
// router.post(AuthRoutes.loginLocal, controller.localStrategy.bind(controller));
// //* /Auth/logout
// router.post(AuthRoutes.logout, controller.logout.bind(controller));

// export const authRouter = router;
